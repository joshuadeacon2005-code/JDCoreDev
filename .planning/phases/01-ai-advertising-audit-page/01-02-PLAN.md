---
phase: 01-ai-advertising-audit-page
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - client/src/components/PublicNavbar.tsx
autonomous: true
requirements:
  - MKTG-NAV-01-AAA

must_haves:
  truths:
    - "PublicNavbar Services dropdown surfaces an 'AI Advertising Audit' entry that navigates to /services/ai-advertising-audit"
    - "Mobile menu surfaces a sub-row under the Services row that navigates to /services/ai-advertising-audit"
    - "When the user is on /services/ai-advertising-audit, the active-state styling fires (cn() helper)"
  artifacts:
    - path: "client/src/components/PublicNavbar.tsx"
      provides: "Top-of-site nav with the AAA entry surfaced"
      contains: "/services/ai-advertising-audit"
  key_links:
    - from: "PublicNavbar `services` array"
      to: "/services/ai-advertising-audit"
      via: "HoveredLink href on desktop dropdown"
      pattern: "href: \"/services/ai-advertising-audit\""
    - from: "PublicNavbar mobile menu"
      to: "/services/ai-advertising-audit"
      via: "<Link href=\"/services/ai-advertising-audit\"> sub-row"
      pattern: "Link href=\"/services/ai-advertising-audit\""
---

<objective>
Wire the new `/services/ai-advertising-audit` route into the site nav. Replace the four placeholder Services-dropdown entries (which all currently point to `/services`) with the two real entries Phase 1 needs: an "All services" item that keeps `/services` reachable, plus the new "AI Advertising Audit" item. Add a sub-row to the mobile menu that surfaces the same route.

Phase 2 will append the SEO entry — leave room but do not pre-add it.

Purpose: Phase 1 success criterion #2 — "the PublicNavbar Services entry exposes the AI Advertising Audit page; clicking it navigates to the new route."

Output: edits to `client/src/components/PublicNavbar.tsx` only.

Closes requirements: MKTG-NAV-01-AAA (AAA portion of the site-wide nav requirement; SEO portion lands in Phase 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-ai-advertising-audit-page/01-UI-SPEC.md
@client/src/components/PublicNavbar.tsx

<interfaces>
<!-- From client/src/components/PublicNavbar.tsx (the file you're editing). -->

Existing imports relevant to this plan (line 7):
`import { Code, Layers, Rocket, Server, Menu as MenuIcon, X } from "lucide-react";`
→ Replace with: `import { Code, Layers, Rocket, Server, Menu as MenuIcon, X, Target } from "lucide-react";`
(Code, Rocket, Server become unused after this plan but DO NOT remove — they may be used elsewhere or in mobile menu future expansion. Linter will warn on unused; that's fine and lower-priority than minimising diff surface.)

Actually — confirm with grep at edit time. If `Code` / `Rocket` / `Server` are unused after the swap, the linter will fail the build. Strategy: only import what's used in the new array, keep `Layers` for "All services", add `Target` for AAA. Drop the others.

Existing `services` array (lines 11-16) — REPLACE wholesale.

Existing mobile menu Services row (lines 102-109) — KEEP as-is, ADD a sub-row immediately after it.

Existing `cn()` helper (imported line 8) — use for active-state styling on both desktop and mobile.

Existing `useLocation()` hook (line 21) and `isActive` helper (line 23) — reuse for active-state highlighting on the new entry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the placeholder `services` array with real entries (All services + AAA)</name>
  <files>client/src/components/PublicNavbar.tsx</files>
  <read_first>
    - .planning/phases/01-ai-advertising-audit-page/01-UI-SPEC.md (section 10a — PublicNavbar plan, including the exact array shape Phase 1 should land)
    - client/src/components/PublicNavbar.tsx (full file — focus on lines 7-16 for the imports + array)
  </read_first>
  <action>
**Step 1 — update the lucide-react import on line 7.**

Current:
```tsx
import { Code, Layers, Rocket, Server, Menu as MenuIcon, X } from "lucide-react";
```

Replace with:
```tsx
import { Layers, Target, Menu as MenuIcon, X } from "lucide-react";
```

(Drop `Code`, `Rocket`, `Server` — they are only used by the placeholder `services` array we are about to remove. Add `Target` for the AAA entry, matching the icon used in UI-SPEC §10a and on the homepage card in Plan 03.)

**Step 2 — replace the `services` array (lines 11-16).**

Current:
```tsx
const services = [
  { href: "/services", label: "Custom Development", icon: Code },
  { href: "/services", label: "Technical Consulting", icon: Layers },
  { href: "/services", label: "MVP Development", icon: Rocket },
  { href: "/services", label: "Managed Hosting", icon: Server },
];
```

Replace with the EXACT shape from UI-SPEC §10a:
```tsx
const services = [
  { href: "/services",                          label: "All services",         icon: Layers },
  { href: "/services/ai-advertising-audit",     label: "AI Advertising Audit", icon: Target },
];
```

**Do NOT** pre-add the SEO entry. Phase 2 will append it.

Do not touch the desktop `MenuItem` / `HoveredLink` rendering loop (lines 41-49) — it iterates `services.map(...)` and works with any length.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -F "PublicNavbar" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "import { Layers, Target, Menu as MenuIcon, X }" client/src/components/PublicNavbar.tsx` returns 1 match.
    - `grep -cE "from \"lucide-react\"" client/src/components/PublicNavbar.tsx` returns 1.
    - `grep -F "label: \"All services\"" client/src/components/PublicNavbar.tsx` returns 1 match.
    - `grep -F "label: \"AI Advertising Audit\"" client/src/components/PublicNavbar.tsx` returns 1 match.
    - `grep -F "href: \"/services/ai-advertising-audit\"" client/src/components/PublicNavbar.tsx` returns 1 match.
    - `grep -cF "icon: " client/src/components/PublicNavbar.tsx` returns 2 (matching the 2 entries).
    - `grep -F "Custom Development" client/src/components/PublicNavbar.tsx` returns 0 matches (placeholder gone).
    - `grep -F "Managed Hosting" client/src/components/PublicNavbar.tsx` returns 0 matches.
    - File compiles under `npx tsc --noEmit`.
  </acceptance_criteria>
  <done>The four placeholder dropdown entries are gone; "All services" + "AI Advertising Audit" are the only two entries. Lucide imports are minimal (only `Layers`, `Target`, `Menu`, `X`). File compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add a mobile-menu sub-row pointing at /services/ai-advertising-audit, indented under the existing Services row</name>
  <files>client/src/components/PublicNavbar.tsx</files>
  <read_first>
    - .planning/phases/01-ai-advertising-audit-page/01-UI-SPEC.md (section 10a — the mobile-menu sub-row pattern, "indented and styled as a sub-item")
    - client/src/components/PublicNavbar.tsx (lines 102-125 for the existing mobile menu rows)
  </read_first>
  <action>
Locate the mobile menu Services row (currently lines 102-109):

```tsx
<Link href="/services" onClick={() => setMobileMenuOpen(false)}>
  <div className={cn(
    "px-4 py-2 rounded-lg transition-colors",
    isActive("/services") ? "bg-primary/10 text-foreground font-medium" : "text-muted-foreground hover:bg-muted"
  )}>
    Services
  </div>
</Link>
```

Insert a NEW sub-row IMMEDIATELY AFTER the closing `</Link>` of the Services row (and before the Work `<Link>` row that starts at line 110). The new block is:

```tsx
<Link href="/services/ai-advertising-audit" onClick={() => setMobileMenuOpen(false)}>
  <div className={cn(
    "pl-8 pr-4 py-2 rounded-lg transition-colors text-xs uppercase tracking-wide",
    isActive("/services/ai-advertising-audit")
      ? "bg-primary/10 text-foreground font-medium"
      : "text-muted-foreground hover:bg-muted"
  )}>
    AI Advertising Audit
  </div>
</Link>
```

Notes on the styling:
- `pl-8` (instead of `px-4`) gives the visual indent that signals "sub-item of the Services row above" — UI-SPEC §10a calls for indent.
- `text-xs uppercase tracking-wide` matches the bullet/feature-list typography pattern from services.tsx (used inside benefit cards) and visually distinguishes a sub-item from the parent Services row's default sizing.
- Reuses the existing `cn()` and `isActive()` helpers — no new imports.
- `setMobileMenuOpen(false)` on click matches the pattern of every other mobile menu row.

Do not touch the existing Services / Work / Contact rows or the Sign In button.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -F "PublicNavbar" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "Link href=\"/services/ai-advertising-audit\"" client/src/components/PublicNavbar.tsx` returns 1 (the mobile sub-row only — desktop dropdown uses `href:` in the array, not a literal `Link href=`).
    - `grep -F "isActive(\"/services/ai-advertising-audit\")" client/src/components/PublicNavbar.tsx` returns 1 match.
    - `grep -F "pl-8" client/src/components/PublicNavbar.tsx` returns 1 match (the indent class on the new sub-row).
    - `grep -F "AI Advertising Audit</div>" client/src/components/PublicNavbar.tsx` returns 1 match (label text inside the mobile sub-row).
    - The original mobile Services row (`Link href="/services"` with `Services` label) is still present unchanged — `grep -F ">Services</div>" client/src/components/PublicNavbar.tsx` returns 1 match.
    - File compiles under `npx tsc --noEmit`.
  </acceptance_criteria>
  <done>Mobile menu now shows: Services → (indented sub-row) AI Advertising Audit → Work → Contact → Sign In. Tapping the sub-row navigates to /services/ai-advertising-audit and closes the menu. Active styling fires when on that route.</done>
</task>

</tasks>

<verification>
- Manual smoke test: run `npm run dev`. Desktop: hover the Services menu → see "All services" and "AI Advertising Audit" entries → click the second → navigates to /services/ai-advertising-audit. Mobile (resize <768px): tap the menu icon → see Services then indented "AI Advertising Audit" → tap it → navigates and menu closes. Visit /services/ai-advertising-audit directly → mobile sub-row shows active styling.
- TypeScript: `cd client && npx tsc --noEmit` passes.
- Build: `npm run build` succeeds.
</verification>

<success_criteria>
- Desktop Services dropdown has exactly 2 entries: "All services" → /services, "AI Advertising Audit" → /services/ai-advertising-audit.
- Mobile menu has an indented sub-row under the Services row that links to /services/ai-advertising-audit.
- Active-state styling fires on both desktop and mobile when on the new route.
- Build succeeds with no TS errors and no unused-import warnings on PublicNavbar.tsx.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-advertising-audit-page/01-02-SUMMARY.md` summarising the diff and noting that the SEO entry is intentionally deferred to Phase 2.
</output>
