---
phase: 01-ai-advertising-audit-page
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - client/src/pages/home.tsx
autonomous: true
requirements:
  - MKTG-HOME-01-AAA

must_haves:
  truths:
    - "Homepage renders a 'Targeted services' section between the existing 'Stuff we've actually built' section and the footer CTA"
    - "Section contains a single AI Advertising Audit card that links to /services/ai-advertising-audit"
    - "Card layout uses md:grid-cols-2 so Phase 2's SEO sibling card slots in cleanly"
  artifacts:
    - path: "client/src/pages/home.tsx"
      provides: "Homepage card section linking to AAA"
      contains: "/services/ai-advertising-audit"
  key_links:
    - from: "client/src/pages/home.tsx"
      to: "/services/ai-advertising-audit"
      via: "<Link href> wrapping the AAA card"
      pattern: "Link href=\"/services/ai-advertising-audit\""
---

<objective>
Add a homepage card that surfaces the new AI Advertising Audit service. UI-SPEC §10b locks the placement (between the existing "Stuff we've actually built" section and the footer CTA) and the card markup. Use a `md:grid-cols-2` container so Phase 2 can drop the SEO sibling card in without re-laying-out.

Purpose: Phase 1 success criterion #3 — "the homepage shows a card for the AI Advertising Audit service that links to the new route."

Output: edits to `client/src/pages/home.tsx` only.

Closes requirements: MKTG-HOME-01-AAA (AAA portion of the homepage-card requirement; SEO portion lands in Phase 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-ai-advertising-audit-page/01-UI-SPEC.md
@client/src/pages/services.tsx
@client/src/pages/home.tsx

<interfaces>
<!-- From client/src/pages/home.tsx -->

Existing imports (lines 1-45):
- `Link` from `wouter` — already imported.
- `AnimatedContainer` — local helper or imported. **Check on edit**: home.tsx may declare its own local AnimatedContainer (services.tsx does). Confirm via grep before adding the new section; reuse whatever the file uses for its other AnimatedContainer-style sections.
- `Target` from `lucide-react` — **NOT currently in the home.tsx import list (lines 13-45)**. Must be added to the existing `lucide-react` import block.
- `ArrowRight` from `lucide-react` — already imported (line 34).

Insertion point:
- The "Stuff we've actually built" `<section className="py-20 border-t bg-muted/30">` opens at line 799 and closes around line 825.
- The footer (`<footer className="bg-card border-t pt-20">`) opens at line 827.
- Insert the NEW section between them — i.e. after the closing `</section>` of "Stuff we've actually built" and before the opening `<footer>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `Target` to the lucide-react import in home.tsx</name>
  <files>client/src/pages/home.tsx</files>
  <read_first>
    - client/src/pages/home.tsx (lines 13-45 for the existing lucide-react import block)
  </read_first>
  <action>
Locate the existing `lucide-react` import block (lines 13-45). It is a multi-line import. Add `Target` to the alphabetically-correct position (between `Stethoscope` and `TrendingUp` — though the existing block is not strictly alphabetical, so just add it alongside other adjacent icons; safe placement is right after `Store` on its own line since that's at the end of the merchant icons cluster).

Smallest-diff approach: insert `Target,` on a new line immediately before the closing `} from "lucide-react";` line.

Before edit (excerpt):
```tsx
  HardHat,
  GraduationCap,
  ChevronRight
} from "lucide-react";
```

After edit:
```tsx
  HardHat,
  GraduationCap,
  ChevronRight,
  Target,
} from "lucide-react";
```

Note the trailing comma added after `ChevronRight` — TypeScript allows trailing commas in import lists, and adding one alongside the new entry makes future inserts cleaner. If `ChevronRight` already has a trailing comma, just insert `Target,` on its own line below it.

Do not touch any other import.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -F "home.tsx" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^  Target,?$" client/src/pages/home.tsx` returns at least 1 match (Target on its own line inside the import block).
    - The total `from "lucide-react"` import count is unchanged at 1: `grep -cF "from \"lucide-react\"" client/src/pages/home.tsx` returns 1.
    - File compiles under `npx tsc --noEmit`.
  </acceptance_criteria>
  <done>`Target` is importable from lucide-react inside home.tsx; no other imports disturbed; file compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Insert the "Targeted services" section between "Stuff we've actually built" and the footer</name>
  <files>client/src/pages/home.tsx</files>
  <read_first>
    - .planning/phases/01-ai-advertising-audit-page/01-UI-SPEC.md (section 10b — full markup for both the section shell and the AAA card)
    - client/src/pages/home.tsx (lines 795-830 — confirms exact insertion point: after the `</section>` closing the "Stuff we've actually built" block at ~line 825, before the `<footer>` at line 827)
  </read_first>
  <action>
Insert the following section IMMEDIATELY AFTER the closing `</section>` of the "Stuff we've actually built" block (currently at ~line 825, just before line 827's `<footer className="bg-card border-t pt-20">`).

The block to insert is verbatim from UI-SPEC §10b — section shell + single AAA card inside a `md:grid-cols-2` grid (Phase 2 will add the SEO sibling so the second cell isn't empty long-term):

```tsx
      <section className="py-20 border-t">
        <div className="max-w-[1400px] mx-auto px-8">
          <AnimatedContainer className="text-center mb-12">
            <span className="text-accent font-black italic uppercase tracking-[0.3em] mb-4 text-sm block">// Targeted services</span>
            <h2 className="text-4xl md:text-6xl font-black tracking-tighter uppercase italic">
              Specific <span className="text-primary">audits & rebuilds</span>
            </h2>
            <p className="text-muted-foreground mt-4 text-lg max-w-2xl mx-auto">
              Standalone services for businesses who already know what they need fixed.
            </p>
          </AnimatedContainer>
          <AnimatedContainer delay={0.2}>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
              <Link href="/services/ai-advertising-audit" className="block border border-border p-8 hover:bg-muted/20 transition-colors group" data-testid="card-home-aaa">
                <div className="flex items-center gap-3 mb-3">
                  <Target className="h-5 w-5 text-primary" />
                  <h3 className="text-xl font-black uppercase italic tracking-tight">AI Advertising Audit</h3>
                </div>
                <p className="text-sm text-muted-foreground leading-relaxed mb-6">
                  A plain-English review of your Google and Meta ads. We find the wasted spend, fix the tracking, and sharpen the creative — so your budget actually pays back.
                </p>
                <span className="text-xs font-bold uppercase tracking-widest text-primary inline-flex items-center gap-1 group-hover:gap-2 transition-all">
                  See the audit <ArrowRight className="h-3 w-3" />
                </span>
              </Link>
              {/* Phase 2 will add the SEO Audit sibling card here */}
            </div>
          </AnimatedContainer>
        </div>
      </section>

```

Indentation note: home.tsx uses 6 spaces of leading indent at the `<section>` level (siblings of the existing sections show this). Match the surrounding indentation exactly when pasting.

The `{/* Phase 2 will add the SEO Audit sibling card here */}` comment is intentional — it locks the insertion point for Phase 2 and makes the half-empty grid intentional during Phase 1.

Do not touch the "Stuff we've actually built" section above or the `<footer>` below.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit 2>&1 | grep -F "home.tsx" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "// Targeted services" client/src/pages/home.tsx` returns 1 match.
    - `grep -F "Specific <span className=\"text-primary\">audits & rebuilds</span>" client/src/pages/home.tsx` returns 1 match.
    - `grep -F "Link href=\"/services/ai-advertising-audit\"" client/src/pages/home.tsx` returns 1 match.
    - `grep -F "data-testid=\"card-home-aaa\"" client/src/pages/home.tsx` returns 1 match.
    - `grep -F "Phase 2 will add the SEO Audit sibling card here" client/src/pages/home.tsx` returns 1 match.
    - `grep -F "See the audit" client/src/pages/home.tsx` returns 1 match.
    - The new section appears AFTER the "Stuff we've actually built" eyebrow and BEFORE the footer: `awk '/Targeted services/{ts=NR} /Stuff we'\''ve <span/{sb=NR} /<footer className="bg-card border-t pt-20">/{f=NR} END{exit !(sb<ts && ts<f)}' client/src/pages/home.tsx` exits 0.
    - File compiles under `npx tsc --noEmit`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Homepage now renders a "Targeted services" section between "Stuff we've actually built" and the footer. The section contains one AAA card linking to /services/ai-advertising-audit. Phase 2 has a marked insertion comment to drop the SEO sibling.</done>
</task>

</tasks>

<verification>
- Manual smoke test: run `npm run dev`, visit `/`, scroll to the bottom, confirm "Targeted services" eyebrow appears between the "Stuff we've actually built" section and the footer. Confirm one card with the Target icon + "AI Advertising Audit" title + "See the audit" CTA. Click the card → navigates to /services/ai-advertising-audit.
- TypeScript: `cd client && npx tsc --noEmit` passes.
- Build: `npm run build` succeeds.
</verification>

<success_criteria>
- Homepage has a new section between "Stuff we've actually built" and the footer.
- Section contains exactly one AAA card linking to /services/ai-advertising-audit.
- Card uses Target icon, "AI Advertising Audit" title, the locked description, and the "See the audit →" CTA.
- Grid is `md:grid-cols-2` with a placeholder comment for Phase 2's SEO card.
- Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-advertising-audit-page/01-03-SUMMARY.md` summarising the new section's location, the lucide-react import diff, and noting the placeholder comment for Phase 2.
</output>
