---
phase: 01-ai-advertising-audit-page
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - server/routes.ts
autonomous: true
requirements:
  - MKTG-MAP-01-AAA

must_haves:
  truths:
    - "GET /sitemap.xml emits a <url> entry whose <loc> is https://www.jdcoredev.com/services/ai-advertising-audit"
    - "The new entry has priority 0.8 and changefreq monthly"
    - "Existing /audits and / entries are preserved"
  artifacts:
    - path: "server/routes.ts"
      provides: "staticUrls array containing the new AAA URL"
      contains: "/services/ai-advertising-audit"
  key_links:
    - from: "server/routes.ts staticUrls array"
      to: "/sitemap.xml output"
      via: "the route handler maps staticUrls into XML entries"
      pattern: "/services/ai-advertising-audit"
---

<objective>
Add the new AAA URL to the dynamic sitemap by appending one entry to the `staticUrls` array around line 435 of `server/routes.ts`. UI-SPEC §10c locks the position (between `/` and `/audits`), priority (0.8), and changefreq (monthly).

Phase 2 will append the SEO entry after this one — leave the array shape predictable.

Purpose: Phase 1 success criterion #4 — "/sitemap.xml includes /services/ai-advertising-audit with sensible priority and changefreq."

Output: edits to `server/routes.ts` only.

Closes requirements: MKTG-MAP-01-AAA (AAA portion of the sitemap requirement; SEO portion lands in Phase 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-ai-advertising-audit-page/01-UI-SPEC.md
@server/routes.ts

<interfaces>
<!-- From server/routes.ts (the file you're editing). -->

The relevant block (currently lines 432-466) is the GET /sitemap.xml handler. The `staticUrls` array is at lines 435-438:

```ts
const staticUrls: SitemapEntry[] = [
  { loc: SITE + "/",         priority: "1.0", changefreq: "weekly" },
  { loc: SITE + "/audits",   priority: "0.8", changefreq: "daily"  },
];
```

The `SitemapEntry` type is declared one line above (line 434):
`type SitemapEntry = { loc: string; priority: string; changefreq: string; lastmod?: string };`

The `SITE` constant is declared at line 433: `const SITE = "https://www.jdcoredev.com";`

`priority` and `changefreq` are STRINGS in this codebase (note the quotes on `"1.0"` and `"weekly"`). Match this pattern — do not introduce numeric literals.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the AAA entry into the staticUrls array</name>
  <files>server/routes.ts</files>
  <read_first>
    - .planning/phases/01-ai-advertising-audit-page/01-UI-SPEC.md (section 10c — sitemap plan including the exact entry shape and rationale for priority 0.8 / changefreq monthly)
    - server/routes.ts (lines 428-466 — the entire sitemap handler block, so the executor sees the array, the type, and the SITE constant)
  </read_first>
  <action>
Locate the `staticUrls` array (currently lines 435-438):

```ts
    const staticUrls: SitemapEntry[] = [
      { loc: SITE + "/",         priority: "1.0", changefreq: "weekly" },
      { loc: SITE + "/audits",   priority: "0.8", changefreq: "daily"  },
    ];
```

Replace it with the 3-entry version, inserting the AAA URL between `/` and `/audits`:

```ts
    const staticUrls: SitemapEntry[] = [
      { loc: SITE + "/",                                  priority: "1.0", changefreq: "weekly"  },
      { loc: SITE + "/services/ai-advertising-audit",     priority: "0.8", changefreq: "monthly" },
      { loc: SITE + "/audits",                            priority: "0.8", changefreq: "daily"   },
    ];
```

Notes on formatting:
- The existing array uses column-aligned spacing on `loc:`, `priority:`, and `changefreq:` for readability — match it. The exact spacing in the UI-SPEC §10c example is the target.
- Quotes around `"0.8"` and `"monthly"` — the existing entries use string literals, not numbers. Match that.
- Do not reorder the existing entries' relative positions — `/` stays first, `/audits` stays last in this array.
- Phase 2 will insert the SEO entry between AAA and `/audits` — that ordering is intentional (alphabetical-ish under `/services/*`).

Do not touch any other part of the sitemap handler (the auditUrls fetch, the XML serialisation, the `res.send`, etc).
  </action>
  <verify>
    <automated>cd "C:\Users\joshu\iCloudDrive\JDCoreDev Code\JDCoreDev" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -F "routes.ts" ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "/services/ai-advertising-audit" server/routes.ts` returns at least 1 match.
    - `grep -F "{ loc: SITE + \"/services/ai-advertising-audit\"" server/routes.ts` returns 1 match.
    - On the same line as the new entry: `grep -F "priority: \"0.8\", changefreq: \"monthly\"" server/routes.ts` returns at least 1 match (the AAA line).
    - The existing entries are preserved: `grep -F "{ loc: SITE + \"/\"," server/routes.ts` returns 1 match (the homepage entry); `grep -F "{ loc: SITE + \"/audits\"" server/routes.ts` returns 1 match.
    - The staticUrls array now has 3 entries: `awk '/const staticUrls: SitemapEntry\[\] = \[/,/^    \];/' server/routes.ts | grep -cE "^\s*\{ loc: SITE \+" ` returns 3.
    - File compiles under `npx tsc --noEmit`.
    - Smoke run: start the server (`npm run dev` in another window) and `curl -s http://localhost:5000/sitemap.xml | grep -F "<loc>https://www.jdcoredev.com/services/ai-advertising-audit</loc>"` returns 1 match. (Adjust port to whatever the dev server uses; alternative: hit `/sitemap.xml` in a browser and use ctrl-F.)
  </acceptance_criteria>
  <done>The sitemap response now contains a `<url>` entry for /services/ai-advertising-audit with `<priority>0.8</priority>` and `<changefreq>monthly</changefreq>`. The homepage and /audits entries are unchanged. Server compiles and runs.</done>
</task>

</tasks>

<verification>
- `npm run dev`, hit `/sitemap.xml`, confirm new `<url>` entry appears.
- TypeScript: `npx tsc --noEmit` passes.
- Build: `npm run build` succeeds.
</verification>

<success_criteria>
- `staticUrls` array has 3 entries including the AAA URL with priority 0.8 / changefreq monthly.
- `/sitemap.xml` response contains the new URL when fetched against the dev server.
- Existing entries unchanged.
- Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-advertising-audit-page/01-04-SUMMARY.md` summarising the array diff and showing the curl-tested sitemap snippet.
</output>
